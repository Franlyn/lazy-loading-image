function DominateColorLoading(url) {
	// fetch the dominate color 
}

function LQIPLoading(url) {
	//
}

function LazyLoading(url, option) {
	switch (option) {
		case 1:
			DominateColorLoading(url);
			break;
		case 2:
			LQIPLoading(url);
	}
}